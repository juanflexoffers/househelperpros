# HouseHelperPros
Lead-gen landing site built with **Next.js** and deployed on **Vercel**.

## Local development

```bash
npm install
npm run dev

Then open: http://localhost:3000

Build
npm run build
npm run start

Notes
Routes live in app/ (Next.js App Router).
Styling is in app/globals.css.

---
### Quick verify
After you commit/push those:
```bash
npm install
npm run build
